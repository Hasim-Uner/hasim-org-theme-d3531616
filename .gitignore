inc/contact-local.php

# Composer (Dev-Tooling: PHPStan, WP-Stubs) — nicht versionieren.
# composer.lock wird bewusst committet: pinnt die PHPStan-Version,
# damit die phpstan-baseline.neon reproduzierbar gültig bleibt.
/vendor/

# D3 Custom-Bundle Build (einmaliger Build-Schritt)
_build-d3/node_modules/
_build-d3/package-lock.json

# Stitch API — Node-Module und Secrets nicht versionieren
_stitch/node_modules/
_stitch/.env
_stitch/package-lock.json

# Lokale TTS-Ausgaben und Secrets nicht versionieren
generated-audio/
.env
*.env
.DS_Store
